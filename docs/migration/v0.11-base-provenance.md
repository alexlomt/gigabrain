# Gigabrain v0.11 Compatibility Base Provenance

Verified on 2026-08-25 UTC.

## Release identity

- Latest stable GitHub release: `v0.11.0`.
- Upstream tag object type: lightweight commit tag.
- Tag commit: `ef624f97cc616a9e00b6df653eded455fbd30e01`.
- Commit tree: `2a7affea1439f4250bcf9a3078ecf0cb36cc3358`.
- Node engine floor: `>=22.18.0`.

## npm publication

- Package: `@legendaryvibecoder/gigabrain@0.11.0`.
- Registry `dist.shasum` (SHA-1): `78fd10e22d4aa4851011cc542465d25f6943dde1`.
- Downloaded tarball SHA-1: `78fd10e22d4aa4851011cc542465d25f6943dde1`.
- Published-file/tag equality: pass for all 100 regular files.
- Published-file manifest SHA-256: `eecba578702b2bf63b0a8dc750b3fbdc4c3e05f80045a120e765da8ba61d2fe9`.

The manifest hash is SHA-256 over the locale-sorted lines `<Git blob SHA-1><two spaces><relative path>\n` for every regular file in the downloaded npm archive. Each archived path was required to exist at `v0.11.0`, and its unfiltered Git blob hash was required to equal the tag blob hash. Non-file archive entries other than directories were rejected.

## Compatibility branch policy

The branch `upgrade/v0.11-openclaw-compat` starts directly from the audited commit above. Its package metadata version is `0.11.0-openclaw.1`. The deployed `0.10.0` line and all historical custom commits are excluded: do not merge, rebase, or cherry-pick them into this branch without a contract-level review.

The worktree was clean at the audited base before the compatibility metadata and this provenance record were added.
